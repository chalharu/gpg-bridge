name: CI

on:
  push:
    branches: [main]
    tags: ['v*']
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  # ---------------------------------------------------------------------------
  # Change detection
  # ---------------------------------------------------------------------------
  changes:
    name: Detect changed areas
    runs-on: ubuntu-latest
    outputs:
      rust: ${{ steps.filter.outputs.rust }}
      flutter: ${{ steps.filter.outputs.flutter }}
      android: ${{ steps.filter.outputs.android }}
      ios: ${{ steps.filter.outputs.ios }}
      docker: ${{ steps.filter.outputs.docker }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Detect changed files
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            rust:
              - 'Cargo.toml'
              - 'Cargo.lock'
              - 'server/**'
              - 'daemon/**'
              - '.github/workflows/ci.yml'
              - '.github/scripts/**'
            flutter:
              - 'mobile/lib/**'
              - 'mobile/test/**'
              - 'mobile/pubspec.*'
              - 'mobile/analysis_options.yaml'
              - '.github/workflows/ci.yml'
              - '.github/scripts/**'
            android:
              - 'mobile/android/**'
              - '.github/workflows/ci.yml'
            ios:
              - 'mobile/ios/**'
              - '.github/workflows/ci.yml'
            docker:
              - 'Dockerfile.server'
              - '.dockerignore'
              - '.github/workflows/ci.yml'

  # ---------------------------------------------------------------------------
  # Rust lint (format + clippy) — runs in parallel with coverage
  # ---------------------------------------------------------------------------
  rust-lint:
    name: Rust lint
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Restore file timestamps
        uses: chetan/git-restore-mtime-action@v2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt,clippy

      # NOTE: This cache key is intentionally shared between rust-lint and
      # rust-coverage. The second writer is a harmless no-op (same immutable key).
      - name: Check Cargo.lock cache
        id: cargo-lock-check
        uses: actions/cache@v4
        with:
          path: /tmp/.cargo-lock-marker
          key: cargo-lock-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}

      - name: Create Cargo.lock marker
        run: mkdir -p /tmp/.cargo-lock-marker && cp Cargo.lock /tmp/.cargo-lock-marker/

      - name: Cache Rust artifacts
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
          cache-all-crates: true

      - name: Clean stale cache on lockfile change
        if: steps.cargo-lock-check.outputs.cache-hit != 'true'
        run: |
          echo 'Cargo.lock changed — cleaning build artifacts'
          cargo clean || true

      - name: Format check
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --all-targets -- -D warnings

      - name: Check line counts (Rust)
        run: python3 .github/scripts/check-line-counts.py --lang rust --max-file-lines 200 --max-method-lines 30 --max-test-file-lines 800 --max-test-method-lines 80 --warn-only server/src daemon/src

  # ---------------------------------------------------------------------------
  # Rust coverage (test + lcov + threshold) — runs in parallel with lint
  # ---------------------------------------------------------------------------
  rust-coverage:
    name: Rust coverage
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    env:
      CARGO_INCREMENTAL: 0
      CARGO_LLVM_COV_TARGET_DIR: target/llvm-cov-target
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Restore file timestamps
        uses: chetan/git-restore-mtime-action@v2

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Check Cargo.lock cache
        id: cargo-lock-check
        uses: actions/cache@v4
        with:
          path: /tmp/.cargo-lock-marker-cov
          key: cargo-lock-cov-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}

      - name: Create Cargo.lock marker
        run: mkdir -p /tmp/.cargo-lock-marker-cov && cp Cargo.lock /tmp/.cargo-lock-marker-cov/

      # Separate cache prefix for coverage builds (instrumented binaries
      # are incompatible with non-instrumented ones in rust-lint).
      - name: Cache Rust artifacts (coverage)
        uses: Swatinem/rust-cache@v2
        with:
          cache-on-failure: true
          cache-all-crates: true
          prefix-key: v0-rust-coverage

      - name: Cache embedded PostgreSQL binaries
        uses: actions/cache@v4
        with:
          path: ~/.theseus/postgresql
          key: embedded-postgres-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}
          restore-keys: |
            embedded-postgres-${{ runner.os }}-

      - name: Clean stale cache on lockfile change
        if: steps.cargo-lock-check.outputs.cache-hit != 'true'
        run: |
          echo 'Cargo.lock changed — cleaning build artifacts'
          cargo clean || true

      - name: Coverage (includes tests, fail under 80%)
        run: cargo llvm-cov --workspace --lcov --output-path lcov-rust.info --fail-under-lines 80 -- --include-ignored

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: lcov-rust.info
          flags: rust
          disable_search: true
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload Rust coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: lcov-rust
          path: lcov-rust.info

  # ---------------------------------------------------------------------------
  # Flutter lint (format + analyze) — runs in parallel with test/coverage
  # ---------------------------------------------------------------------------
  flutter-lint:
    name: Flutter lint
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.flutter == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        working-directory: mobile
        run: flutter pub get

      - name: Format check
        working-directory: mobile
        run: dart format --output=none --set-exit-if-changed lib test

      - name: Analyze
        working-directory: mobile
        run: flutter analyze

      - name: Check line counts (Dart)
        run: python3 .github/scripts/check-line-counts.py --lang dart --max-file-lines 200 --max-method-lines 30 --max-test-file-lines 800 --max-test-method-lines 80 --warn-only mobile/lib mobile/test

  # ---------------------------------------------------------------------------
  # Flutter coverage (test + lcov + threshold) — parallel with lint
  # ---------------------------------------------------------------------------
  flutter-coverage:
    name: Flutter coverage
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.flutter == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        working-directory: mobile
        run: flutter pub get

      - name: Tests with coverage
        working-directory: mobile
        run: flutter test --coverage

      - name: Check coverage threshold (80%)
        run: |
          TOTAL=$(awk -F: '/^LF:/{lf+=$2} /^LH:/{lh+=$2} END{if(lf>0) printf "%.1f", lh/lf*100; else print "0.0"}' mobile/coverage/lcov.info)
          echo "Flutter coverage: ${TOTAL}%"
          awk "BEGIN{exit (${TOTAL} < 80.0) ? 1 : 0}"

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: mobile/coverage/lcov.info
          flags: flutter
          disable_search: true
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload Flutter coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: lcov-flutter
          path: mobile/coverage/lcov.info

  # ---------------------------------------------------------------------------
  # Android native tests (Kotlin JUnit) — parallel with other gates
  # ---------------------------------------------------------------------------
  android-native-test:
    name: Android native tests
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.android == 'true' || needs.changes.outputs.flutter == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('mobile/android/**/*.gradle*', 'mobile/android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Flutter pub get (generates required files)
        working-directory: mobile
        run: flutter pub get

      - name: Configure Android build
        working-directory: mobile
        run: flutter build apk --config-only

      - name: Run Android unit tests
        working-directory: mobile/android
        run: ./gradlew app:testDebugUnitTest

      - name: Generate Android coverage report
        working-directory: mobile/android
        run: ./gradlew app:jacocoTestReport

      - name: Stage Android coverage report
        run: |
          mkdir -p .artifacts/android
          REPORT=mobile/build/app/reports/jacoco/test/jacocoTestReport.xml
          if [ ! -f "$REPORT" ]; then
            echo '::error::Android JaCoCo XML report was not generated'
            find mobile/build -maxdepth 8 -type f | sort | grep -E 'jacoco|test.*xml' || true
            exit 1
          fi
          cp "$REPORT" .artifacts/android/jacocoTestReport.xml
          grep -q '<report' .artifacts/android/jacocoTestReport.xml

      - name: Upload Android coverage to Codecov
        uses: codecov/codecov-action@v5
        with:
          files: .artifacts/android/jacocoTestReport.xml
          flags: android
          disable_search: true
          token: ${{ secrets.CODECOV_TOKEN }}

      - name: Upload Android coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-android
          path: .artifacts/android/jacocoTestReport.xml
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # iOS native tests (XCTest) — parallel with other gates
  # ---------------------------------------------------------------------------
  ios-native-test:
    name: iOS native tests
    runs-on: macos-latest
    needs: changes
    if: needs.changes.outputs.ios == 'true' || needs.changes.outputs.flutter == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Flutter pub get (generates required files)
        working-directory: mobile
        run: flutter pub get

      - name: Cache CocoaPods
        uses: actions/cache@v4
        with:
          path: mobile/ios/Pods
          key: pods-${{ runner.os }}-${{ hashFiles('mobile/ios/Podfile', 'mobile/pubspec.lock') }}
          restore-keys: |
            pods-${{ runner.os }}-

      - name: Install CocoaPods dependencies
        working-directory: mobile/ios
        run: pod install

      - name: Cache Xcode DerivedData
        uses: actions/cache@v4
        with:
          path: mobile/ios/DerivedData
          key: deriveddata-${{ runner.os }}-${{ hashFiles('mobile/ios/Podfile', 'mobile/pubspec.lock', 'mobile/ios/Runner.xcodeproj/project.pbxproj') }}
          restore-keys: |
            deriveddata-${{ runner.os }}-

      - name: Build for testing
        working-directory: mobile/ios
        run: |
          xcodebuild build-for-testing \
            -workspace Runner.xcworkspace \
            -scheme Runner \
            -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' \
            -derivedDataPath DerivedData \
            -enableCodeCoverage YES \
            CODE_SIGNING_ALLOWED=NO \
            -quiet

      - name: Run iOS unit tests (without rebuild)
        working-directory: mobile/ios
        run: |
          xcodebuild test-without-building \
            -workspace Runner.xcworkspace \
            -scheme Runner \
            -destination 'platform=iOS Simulator,name=iPhone 16,OS=latest' \
            -only-testing:RunnerTests \
            -resultBundlePath TestResults \
            -derivedDataPath DerivedData \
            -enableCodeCoverage YES \
            CODE_SIGNING_ALLOWED=NO

      - name: Convert Xcode coverage to lcov
        working-directory: mobile/ios
        run: |
          PROFDATA=$(find DerivedData/Build/ProfileData -name 'Coverage.profdata' -type f 2>/dev/null | head -1)
          if [ -z "$PROFDATA" ]; then
            PROFDATA=$(find TestResults.xcresult -name '*.profdata' -type f 2>/dev/null | head -1)
          fi
          BINARY=$(find DerivedData/Build/Products/Debug-iphonesimulator -name 'Runner' -type f -perm /111 2>/dev/null | grep -v '.dSYM' | head -1)
          if [ -z "$PROFDATA" ] || [ -z "$BINARY" ]; then
            echo "::warning::iOS coverage data not found (profdata=${PROFDATA:-none}, binary=${BINARY:-none})"
            exit 0
          fi
          xcrun llvm-cov export -format=lcov -instr-profile="$PROFDATA" "$BINARY" > ../../lcov-ios.info
          echo "iOS coverage exported: $(wc -l < ../../lcov-ios.info) lines"

      - name: Upload iOS coverage to Codecov
        if: hashFiles('lcov-ios.info') != ''
        uses: codecov/codecov-action@v5
        with:
          files: lcov-ios.info
          flags: ios
          disable_search: true
          token: ${{ secrets.CODECOV_TOKEN }}

  # ---------------------------------------------------------------------------
  # Security audit (Rust) — parallel with other gates
  # ---------------------------------------------------------------------------
  security-audit:
    name: Security audit (Rust)
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-audit
        uses: taiki-e/install-action@cargo-audit

      - name: Run cargo audit
        run: python3 .github/scripts/check-cargo-audit.py --root .

  # ---------------------------------------------------------------------------
  # Dependency check (Dart) — parallel with other gates
  # ---------------------------------------------------------------------------
  dart-deps:
    name: Dependency check (Dart)
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.flutter == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Install dependencies
        working-directory: mobile
        run: flutter pub get

      - name: Check Dart dependencies
        run: python3 .github/scripts/check-dart-deps.py --project-dir mobile

  # ---------------------------------------------------------------------------
  # SQL migration check — parallel with other gates
  # ---------------------------------------------------------------------------
  migration-check:
    name: SQL migration check
    runs-on: ubuntu-latest
    needs: changes
    if: needs.changes.outputs.rust == 'true'
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate migrations
        run: python3 .github/scripts/check-migrations.py server/migrations

  # ---------------------------------------------------------------------------
  # Docker build & push
  # NOTE: This job intentionally does NOT depend on security-audit or
  # migration-check. Those jobs are informational/advisory and should not
  # block Docker image builds.
  # ---------------------------------------------------------------------------
  docker:
    name: Docker build & push
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    needs: [rust-lint, rust-coverage, changes]
    if: >-
      always()
      && (needs.changes.outputs.docker == 'true' || needs.changes.outputs.rust == 'true')
      && (needs.rust-lint.result == 'success' || needs.rust-lint.result == 'skipped')
      && (needs.rust-coverage.result == 'success' || needs.rust-coverage.result == 'skipped')
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU (multi-arch)
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to ghcr.io
        if: github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch')
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/chalharu/gpg-bridge-server
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha,prefix=sha-,format=short
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.server
          platforms: linux/amd64,linux/arm64
          push: ${{ github.ref == 'refs/heads/main' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch') }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ---------------------------------------------------------------------------
  # Android APK build — GitHub Release upload
  # ---------------------------------------------------------------------------
  android-apk-build:
    name: Android APK build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      ANDROID_KEYSTORE_BASE64: ${{ secrets.ANDROID_KEYSTORE_BASE64 }}
      ANDROID_KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
      ANDROID_KEYSTORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
      ANDROID_KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
    needs: [android-native-test, flutter-lint, flutter-coverage, changes]
    if: >-
      always()
      && (needs.changes.outputs.android == 'true' || needs.changes.outputs.flutter == 'true')
      && (needs.android-native-test.result == 'success' || needs.android-native-test.result == 'skipped')
      && (needs.flutter-lint.result == 'success' || needs.flutter-lint.result == 'skipped')
      && (needs.flutter-coverage.result == 'success' || needs.flutter-coverage.result == 'skipped')
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        if: needs.flutter-coverage.result != 'success'
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('mobile/android/**/*.gradle*', 'mobile/android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            gradle-${{ runner.os }}-

      - name: Install dependencies
        working-directory: mobile
        run: flutter pub get

      - name: Compute Android build version
        id: apk_version
        run: |
          SHORT_SHA="$(git rev-parse --short=7 HEAD)"
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            BUILD_NAME="${GITHUB_REF_NAME#v}"
          else
            BUILD_NAME="0.0.0-sha.${SHORT_SHA}"
          fi
          BUILD_NUMBER="$((16#${SHORT_SHA}))"
          APK_NAME="gpg-bridge-${BUILD_NAME}.apk"
          {
            echo "build_name=${BUILD_NAME}"
            echo "build_number=${BUILD_NUMBER}"
            echo "apk_name=${APK_NAME}"
          } >> "$GITHUB_OUTPUT"

      - name: Configure Android release signing
        if: env.ANDROID_KEYSTORE_BASE64 != '' && env.ANDROID_KEY_ALIAS != '' && env.ANDROID_KEYSTORE_PASSWORD != '' && env.ANDROID_KEY_PASSWORD != ''
        working-directory: mobile/android
        run: |
          printf '%s' "$ANDROID_KEYSTORE_BASE64" | base64 --decode > release-keystore.jks
          cat > key.properties <<EOF
          storeFile=release-keystore.jks
          storePassword=$ANDROID_KEYSTORE_PASSWORD
          keyAlias=$ANDROID_KEY_ALIAS
          keyPassword=$ANDROID_KEY_PASSWORD
          EOF

      - name: Build APK (release)
        working-directory: mobile
        run: |
          flutter build apk --release \
            --build-name "${{ steps.apk_version.outputs.build_name }}" \
            --build-number "${{ steps.apk_version.outputs.build_number }}"

      - name: Rename APK output
        run: |
          mv mobile/build/app/outputs/flutter-apk/app-release.apk \
            "mobile/build/app/outputs/flutter-apk/${{ steps.apk_version.outputs.apk_name }}"

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: mobile/build/app/outputs/flutter-apk/${{ steps.apk_version.outputs.apk_name }}

      - name: Upload APK to GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: mobile/build/app/outputs/flutter-apk/${{ steps.apk_version.outputs.apk_name }}

  # ---------------------------------------------------------------------------
  # SonarCloud analysis
  # ---------------------------------------------------------------------------
  sonarcloud:
    name: SonarCloud analysis
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    env:
      GITHUB_TOKEN: ${{ github.token }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
    needs: [rust-coverage, flutter-coverage, android-native-test, changes]
    if: >-
      always()
      && (needs.changes.outputs.rust == 'true' || needs.changes.outputs.flutter == 'true' || needs.changes.outputs.android == 'true')
      && (needs.rust-coverage.result == 'success' || needs.rust-coverage.result == 'skipped')
      && (needs.flutter-coverage.result == 'success' || needs.flutter-coverage.result == 'skipped')
      && (needs.android-native-test.result == 'success' || needs.android-native-test.result == 'skipped')
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download Rust coverage artifact
        if: needs.rust-coverage.result == 'success'
        uses: actions/download-artifact@v4
        with:
          name: lcov-rust
          path: .
        continue-on-error: true

      - name: Download Flutter coverage artifact
        if: needs.flutter-coverage.result == 'success'
        uses: actions/download-artifact@v4
        with:
          name: lcov-flutter
          path: mobile/coverage
        continue-on-error: true

      - name: Download Android coverage artifact
        if: needs.android-native-test.result == 'success'
        uses: actions/download-artifact@v4
        with:
          name: jacoco-android
          path: .artifacts/android
        continue-on-error: true

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Rust
        if: needs.rust-coverage.result != 'success'
        uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview

      - name: Install cargo-llvm-cov
        if: needs.rust-coverage.result != 'success'
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Download Android coverage artifact
        if: needs.android-native-test.result == 'success'
        uses: actions/download-artifact@v4
        with:
          name: jacoco-android
          path: .artifacts/android
        continue-on-error: true

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Generate fallback Rust coverage for Sonar
        if: needs.rust-coverage.result != 'success'
        run: |
          # Sonar cannot carry forward the previous Rust lcov report by itself.
          # When the dedicated rust-coverage job is skipped on Flutter-only PRs,
          # generate a fresh report here so Rust coverage remains visible.
          # This fallback is not the quality gate for Rust changes.
          cargo llvm-cov --workspace --lcov --output-path lcov-rust.info -- --include-ignored

      - name: Flutter pub get (for Sonar analysis)
        if: needs.flutter-coverage.result != 'success'
        working-directory: mobile
        run: flutter pub get

      - name: Generate fallback Flutter coverage for Sonar
        if: needs.flutter-coverage.result != 'success'
        working-directory: mobile
        run: |
          # Sonar cannot carry forward the previous Dart lcov report by itself.
          # When the dedicated flutter-coverage job is skipped on Rust-only PRs,
          # generate a fresh report here so mobile coverage remains visible.
          # This fallback is not the quality gate for Flutter changes.
          flutter test --coverage
          TOTAL=$(awk -F: '/^LF:/{lf+=$2} /^LH:/{lh+=$2} END{if(lf>0) printf "%.1f", lh/lf*100; else print "0.0"}' coverage/lcov.info)
          echo "Flutter fallback coverage: ${TOTAL}%"

      - name: Ensure Android coverage report for Sonar
        if: needs.changes.outputs.android == 'true' || needs.changes.outputs.flutter == 'true'
        run: |
          mkdir -p mobile/build/app/reports/jacoco/test
          if [ -f .artifacts/android/jacocoTestReport.xml ]; then
            cp .artifacts/android/jacocoTestReport.xml mobile/build/app/reports/jacoco/test/jacocoTestReport.xml
            grep -q '<report' mobile/build/app/reports/jacoco/test/jacocoTestReport.xml
            exit 0
          fi
          if [ -f mobile/build/app/reports/jacoco/test/jacocoTestReport.xml ]; then
            exit 0
          fi
          cd mobile
          flutter build apk --config-only
          cd android
          ./gradlew app:testDebugUnitTest
          ./gradlew app:jacocoTestReport
          REPORT=../build/app/reports/jacoco/test/jacocoTestReport.xml
          if [ ! -f "$REPORT" ]; then
            echo '::error::Android JaCoCo XML report was not generated for Sonar'
            find ../build -maxdepth 8 -type f | sort | grep -E 'jacoco|test.*xml' || true
            exit 1
          fi
          grep -q '<report' "$REPORT"

      - name: Verify Android coverage report for Sonar
        if: needs.changes.outputs.android == 'true' || needs.changes.outputs.flutter == 'true'
        run: |
          REPORT=mobile/build/app/reports/jacoco/test/jacocoTestReport.xml
          if [ ! -f "$REPORT" ]; then
            echo '::error::Android JaCoCo XML report was not generated for Sonar'
            find mobile/build -maxdepth 8 -type f | sort | grep -E 'jacoco|test.*xml' || true
            exit 1
          fi
          grep -q '<report' "$REPORT"

      - name: SonarCloud Scan
        uses: SonarSource/sonarqube-scan-action@v6
