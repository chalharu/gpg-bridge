# gpg-bridge daemon

`daemon/` はユーザー環境で動作するローカルデーモンを提供する Rust バイナリです。

## 役割

- サーバーとの接続を維持し、署名・認証・復号などの要求イベントを受信する
- ローカルの秘密鍵操作を仲介し、処理結果をサーバーへ返却する

## 現在の状態

- Cargo プロジェクトの初期スキャフォールドを作成済み
- 実装は未着手（`src/main.rs` は最小テンプレート）
