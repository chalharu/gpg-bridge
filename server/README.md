# gpg-bridge server

`server/` は gpg-bridge のバックエンド API を提供する Rust バイナリです。

## 役割

- ペアリング、署名要求、イベント配信などの API エンドポイントを提供する
- 永続化データと連携し、クライアントとデーモン間の仲介を行う

## 現在の状態

- Cargo プロジェクトの初期スキャフォールドを作成済み
- 実装は未着手（`src/main.rs` は最小テンプレート）
